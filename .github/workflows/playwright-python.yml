name: Playwright Python Tests

on:
  workflow_dispatch:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps

      - name: Run smoke suite
        env:
          ENV: staging
          HEADLESS: "true"
          CUSTOMER_EMAIL: "suraj.biswas@webviotechnologies.com"
          CUSTOMER_PASSWORD: SurajBiswas@1995
        run: pytest -m smoke -n auto

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: automation-reports
          path: |
            reports/
            screenshots/
            logs/
